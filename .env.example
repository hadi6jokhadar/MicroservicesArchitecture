# Copy this file to .env and fill in real values — never commit .env
# Generate secure random values for all secrets before use.

# ── Docker Hub (used by docker-compose.yml image: refs and docker/build-and-push.mjs) ─
DOCKERHUB_USERNAME=your_dockerhub_username

# ── CORS (single source of truth for the deployment's public hostname — see Doc/DOCKER_DEPLOYMENT_GUIDE.md) ─
# Comma-separated, no spaces. Set this to every real frontend origin for THIS server: admin
# (no port, e.g. http://your-hostname), nasheed-admin (:8081), nasheed-web (:8082), both http and
# https if unsure which is live, and http://localhost if you test a locally-run Docker frontend
# image against this backend. Update only this value when moving to a new server/hostname.
CORS_EXTRA_ORIGINS=http://your-hostname,https://your-hostname,http://your-hostname:8081,https://your-hostname:8081,http://your-hostname:8082,https://your-hostname:8082

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=your_postgres_password_here
POSTGRES_REPLICATION_PASSWORD=your_replication_password_here

# ── Redis (docker-compose.redis.yml host-dev-mode instance) ───────────────────
REDIS_PASSWORD=your_redis_password_here

# ── PgAdmin ───────────────────────────────────────────────────────────────────
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=your_pgadmin_password_here

# ── Grafana ───────────────────────────────────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_grafana_password_here
